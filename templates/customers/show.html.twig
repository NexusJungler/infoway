{% extends 'base.html.twig' %}

{% block title %}Customer{% endblock %}

{% block body %}
    <h1>Customer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ customer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ customer.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ customer.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_customers_index') }}">back to list</a>

    <a href="{{ path('admin_customers_edit', {'id': customer.id}) }}">edit</a>

    {{ include('admin_customers/_delete_form.html.twig') }}
{% endblock %}
