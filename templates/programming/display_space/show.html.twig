{% extends 'base.html.twig' %}

{% block title %}DisplaySpace{% endblock %}

{% block body %}
    <h1>DisplaySpace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ display_space.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ display_space.name }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('programming/display_space/_form.html.twig') }}
    <a href="{{ path('programming_display_space_index') }}">back to list</a>

    <a href="{{ path('programming_display_space_edit', {'id': display_space.id}) }}">edit</a>

    {{ include('programming/display_space/_delete_form.html.twig') }}
{% endblock %}
