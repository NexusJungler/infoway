{% extends 'base.html.twig' %}

{% block title %}DisplayMould index{% endblock %}

{% block body %}
    <h1>DisplayMould index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>ScreensNumber</th>
                <th>StartAt</th>
                <th>EndAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for display_mould in display_moulds %}
            <tr>
                <td>{{ display_mould.id }}</td>
                <td>{{ display_mould.name }}</td>
                <td>{{ display_mould.screensNumber }}</td>
                <td>{{ display_mould.startAt ? display_mould.startAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ display_mould.endAt ? display_mould.endAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('programming_display_mould_show', {'id': display_mould.id}) }}">show</a>
                    <a href="{{ path('programming_display_mould_edit', {'id': display_mould.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('programming_display_mould_new') }}">Create new</a>
{% endblock %}
