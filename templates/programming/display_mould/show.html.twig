{% extends 'base.html.twig' %}

{% block title %}DisplayMould{% endblock %}

{% block body %}
    <h1>DisplayMould</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ display_mould.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ display_mould.name }}</td>
            </tr>
            <tr>
                <th>ScreensNumber</th>
                <td>{{ display_mould.screensNumber }}</td>
            </tr>
            <tr>
                <th>StartAt</th>
                <td>{{ display_mould.startAt ? display_mould.startAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ display_mould.endAt ? display_mould.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('programming_display_mould_index') }}">back to list</a>

    <a href="{{ path('programming_display_mould_edit', {'id': display_mould.id}) }}">edit</a>

    {{ include('programming/display_mould/_delete_form.html.twig') }}
{% endblock %}
