{% extends 'base.html.twig' %}

{% block title %}DisplaySetting{% endblock %}

{% block body %}
    <h1>DisplaySetting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ display_setting.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ display_setting.name }}</td>
            </tr>
            <tr>
                <th>ScreensQuantity</th>
                <td>{{ display_setting.screensQuantity }}</td>
            </tr>
        </tbody>
    </table>

    {{ form_start(form) }}
         {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form)  }}
    <a href="{{ path('programming_display_setting_index') }}">back to list</a>

    <a href="{{ path('programming_display_setting_edit', {'id': display_setting.id}) }}">edit</a>

    {{ include('programming/display_setting/_delete_form.html.twig') }}
{% endblock %}
