{% extends 'base.html.twig' %}

{% block title %}New Tag{% endblock %}

{% block body %}
 <main class="main main-tags">
        <div class="main-content">

            <section class="jumbotron">
            
                {% block settingtitle %}
                    {% include 'home/home-info.html.twig' with { 'pageTitle': 'Configuration', 'sousTitle': 'Tags', 'showSecondTitles': true } %}
                {% endblock %}

               {% block wrapper %}
                    {% include 'wrapper/wrapper-settings.html.twig'  %}
                {% endblock %}
                
            </section>

            {% block maincontent %}
                <section class="wrapper_tabcontent wrapper-tags">
                    <div id="setting-tags" class="tab-content current edit-tags">

                        <article class="tab-content-title">
                            <h3 class="title">Modifier TAG</h3>
                        </article>

                        <article class="display setting-display ">
                            <div class="display-content">
                                <div class="display-btn">
{#                                    <a href="{{ path('tags_new') }}"  class="btn" ><i class="fas fa-plus"></i>Créer TAG</a>#}
{#                                    <button type="button" class="btn duplicate-tag"><a href=""><i class="fas fa-plus"></i>Dupliquer</a></button>#}
{#                                    <button type="button" class="btn modified-tag"><i class="far fa-edit"></i>Modifier</button>#}
{#                                    <button type="button" class="btn delete-table" data-tablelistdelete="site"><i class="fas fa-trash"></i>Supprimer</button>#}
                                    {# {{ include('settings/tags/_delete_form.html.twig') }} #}
                                </div>
                            </div>
                        </article>

                        <article class="content-edit-tags">
                            {{ include('settings/tags/_edit_form.html.twig', {'button_label': 'Enregistrer'}) }}
                        </article>
                        
                        {# {{ include('tags/_form.html.twig', {'button_label': 'Update'}) }} #}

                        


                    </div>
                </section>
            {% endblock %}
                
        </div>
    </main>

    
{% endblock %}
