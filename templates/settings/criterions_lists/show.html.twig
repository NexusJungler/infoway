{% extends 'base.html.twig' %}

{% block title %}CriterionList{% endblock %}

{% block body %}
    <h1>CriterionList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ criterions_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ criterions_list.name }}</td>
            </tr>
            <tr>
                <th>Multiple</th>
                <td>{{ criterions_list.multiple ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ criterions_list.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('criterions_lists_index') }}">back to list</a>

    <a href="{{ path('criterions_lists_edit', {'id': criterions_list.id}) }}">edit</a>

    {{ include('settings/criterions_lists/_delete_form.html.twig') }}
{% endblock %}
