<form method="post" action="{{ path('criterions_lists_delete', {'id': criterions_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ criterions_list.id) }}">
    <button class="btn">Delete</button>
</form>