{% extends 'base.html.twig' %}

{% block body %}
    <main class="main home-products">

        <div class="main-content">

            <section class="jumbotron">
                {% include 'home/home-info.html.twig' with { 'pageTitle': 'Produits / Prix' } %}
            </section>

            <section class="wrapper">
                <article class="wrapper-content">
                    <ul class="tabs">
                        <a href="{{ path('app:image') }}"><li class="tablinks active {% if app.request.attributes.get('_route') == 'app:image' %}  current {% endif %}" data-tab="tab-products"> <p class="css-tab__label">Produits / prix</p></li></a>
                        
                        {# <a href="{{ path('app:image') }}"><li class="tablinks active {% if app.request.attributes.get('_route') == 'app:image' %}  current {% endif %}" data-tab="tab-categories"> <p class="css-tab__label">Catégories</p></li></a>
                        <a href="{{ path('app:image') }}"><li class="tablinks active {% if app.request.attributes.get('_route') == 'app:image' %}  current {% endif %}" data-tab="tab-price"> <p class="css-tab__label">Groupe de Prix</p></li></a>
                        <a href="{{ path('app:image') }}"><li class="tablinks active creer {% if app.request.attributes.get('_route') == 'app:image' %}  current {% endif %}" data-tab="tab-create-date"> <p class="css-tab__label">Créer DATE</p></li></a> #}
                    </ul>
                </article>
            </section> 

        <section class="wrapper_tabcontent">

            <article id="tab-products" class="tab-content current">
                {% include 'products/products_price.html.twig' %} 
            </article>
                
            <article id="tab-categories" class="tab-content create-brand tab-create">
                 {% include 'products/products_price.html.twig' %}
            </article>

            <article id="tab-price" class="tab-content  tab-create">
            </article>

            <article id="tab-create-date" class="tab-content  tab-create">
            </article>
            
        </section>    


        </div>

    </main>
{% endblock %}